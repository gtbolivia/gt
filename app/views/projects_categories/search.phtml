<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("projects_categories/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("projects_categories/new", "Create ")); ?>
        </td>
    <tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
            <th>Project</th>
            <th>Category</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $projects_categorie) { ?>
        <tr>
            <td><?php echo $projects_categorie->id ?></td>
            <td><?php echo $projects_categorie->project_id ?></td>
            <td><?php echo $projects_categorie->category_id ?></td>
            <td><?php echo $this->tag->linkTo(array("projects_categories/edit/" . $projects_categorie->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("projects_categories/delete/" . $projects_categorie->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("projects_categories/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("projects_categories/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("projects_categories/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("projects_categories/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    <tbody>
</table>
